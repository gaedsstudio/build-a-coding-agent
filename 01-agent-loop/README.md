# 01 — The conversation loop

Now the model gets memory within one terminal session.

This still is not a tool-using agent, but it introduces the structure every later chapter will keep: **state + loop + stop condition**.

## Run

```bash
python agent.py
```

Type `/exit` to quit.
